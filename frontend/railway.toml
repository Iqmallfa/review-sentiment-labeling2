[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node_modules/.bin/serve dist --listen $PORT --single"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
